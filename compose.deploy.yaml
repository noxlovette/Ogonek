services:
  svelte:
    image: ghcr.io/noxlovette/fl-prod-svelte:v1.1.2
    container_name: svelte-fl-prod
    env_file:
      - ./.env.svelte
    ports:
      - "3333:3000"
    networks:
      - web
      - internal
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
    depends_on:
      postgres:
        condition: service_healthy
  redis:
    image: redis/redis-stack-server:latest
    container_name: redis-fl-prod
    networks:
      - web
      - internal

  axum:
    image: ghcr.io/noxlovette/fl-prod-axum:v1.1.2
    container_name: axum-fl-prod
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    env_file:
      - ./.env.axum
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G

  upld:
    image: ghcr.io/noxlovette/fl-prod-upld:v1.0.2
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    container_name: upld-fl-prod
    networks: 
      - internal
    volumes:
      - uploads:/app/uploads
    env_file:
      - ./.env.upld

  postgres:
    image: postgres:17-alpine
    container_name: pg-fl-prod
    restart: unless-stopped
    env_file: ./.env.postgres
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 2G

volumes:
  db-data:
    name: db-fl-prod
  uploads:
    name: uploads-fl-prod

networks:
  web:
    name: web-fl
  internal:
    name: internal-fl
    internal: true
